# 2026-09-05 02:17 - SC-06 HTTP Contract Coverage

## Status
- [ ] Pending review
- [x] Implemented

## Objective
Add specification-first HTTP endpoint and response contract coverage for the Activity Ranking API.

## Related Functionality
- Feature/module: `e2e-tests/SC-06.*` and `src/Api/ActivityRankingApiClient.ts`
- Trigger: user request / implementation plan

## What Changed
- Added exactly seven tagged SC-06 feature scenarios for endpoint, method, query, status, content type, and error behavior.
- Added individual G2F-generated spec and Steps pairs with shared global bindings.
- Added raw HTTP response support without weakening existing high-level client methods.
- Added scenario-scoped raw response storage and reusable HTTP assertions.
- Added SUT-only API contract execution and documented the intentional red state.

## Files Changed
| File | Change |
|------|--------|
| `e2e-tests/SC-06.*.feature` | Added the seven requested API contract scenarios. |
| `src/Tests/specs/SC-06*.spec.ts` | Added generated and shared native Cucumber bindings. |
| `src/Tests/Steps/SC-06*.steps.ts` | Added raw request orchestration and response validations. |
| `src/Api/ActivityRankingApiClient.ts` | Added centralized raw request construction and response capture. |
| `src/Api/ActivityRankingHttpAssertions.ts` | Added lightweight reusable HTTP assertions. |
| `src/Api/ActivityRankingErrors.ts` | Preserved typed errors and added method-aware diagnostics. |
| `src/Support/CucumberWorld.ts` | Added the `httpResponse` scenario data key. |
| `package.json` | Added `test:api-contract` and excluded API-contract scenarios from fixtures. |
| `README.md` | Documented SC-06 assumptions, statuses, and execution. |

## Implementation Details
The raw `sendRequest()` method returns 400, 404, and 405 responses for direct assertions while continuing to throw typed transport errors when no HTTP response exists. Repeated Cucumber phrases are registered once in the shared SC-06 binding module. The fixture client remains unchanged and SC-06 does not use fake HTTP behavior.

## Results
- Expected outcome: SC-01–SC-05 remain green; SC-06 reaches the configured SUT HTTP boundary.
- Actual outcome: 16 fixture scenarios pass; all seven SC-06 scenarios fail only because the SUT is absent and localhost:3000 refuses the connection.

## Validation
| Check | Command / action | Result |
|------|-------|--------|
| Typecheck | `npm run typecheck` | pass |
| Lint | `npm run lint` | pass with 9 pre-existing console warnings |
| Fixture suite | `npm run test:fixture` | pass: 16 scenarios, 52 steps |
| Repository validation | `npm run validate` | pass |
| API contract suite | `npm run test:api-contract` | expected red: 7 transport failures |
| Raw HTTP smoke check | ephemeral local HTTP server | pass |
| G2F generation | seven individual `npm run g2f -- ...` commands | pass |

## Behavior Impact
- Business behavior and fixture-backed SC-01–SC-05 scenarios are unchanged.
- Developers can target the future HTTP contract with `npm run test:api-contract`.

## Risks and Follow-ups
- The assumed statuses and error wording require confirmation when the production SUT or formal API contract becomes available.
